# Private key for contract deployment
# ⚠️ NEVER commit this file with real values to version control
# Generate a test key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PRIVATE_KEY=add_your_private_key_here
